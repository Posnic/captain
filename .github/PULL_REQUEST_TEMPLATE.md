## What this changes

## Why

<!-- The problem it solves, not a restatement of the diff. If it fixes an
     issue, link it. -->

## How it was checked

<!-- `npx playwright test` at least. If it touches the build or a native
     platform, say which you built and on what. -->

- [ ] `npx playwright test` passes
- [ ] Checked on a real device, or said below why not
